# PR-01 · §3.1 1인칭 "우리" 제거

**심각도:** 🔴 Critical
**건수:** 2
**규칙:** `heritage-v1.md §3.1` "우리의 / 우리는 (브랜드 1인칭) — 광고톤 금지"
**대체:** `철연은 / BYTEFORCE는` 또는 `귀사 / 도입 기업 / 자사`

---

## 변경 1 / 2 · 랜딩 페이지 ROI 섹션 H2

**파일:** `src/app/page.tsx`
**현재 (line 304):**

```tsx
<h2 className="text-3xl md:text-5xl font-[900] text-[#FAFAFA] mb-3" style={{ letterSpacing: "-0.03em" }}>
  우리 회사 기준 절감액
</h2>
```

**수정 후:**

```tsx
<h2 className="text-3xl md:text-5xl font-[900] text-[#FAFAFA] mb-3" style={{ letterSpacing: "-0.03em" }}>
  귀사 기준 절감액
</h2>
```

**근거:** 브랜드 1인칭 "우리"는 소비자 광고톤. B2B 엔터프라이즈 컨텍스트에서 `귀사`는 판결문·감정평가서 어휘와 일관됨.

---

## 변경 2 / 2 · FAQ 질문 (Copy 중앙 파일)

**파일:** `src/content/copy.ts`
**현재 (line 159):**

```ts
{ q: "우리 회사 브랜드로 커스터마이징이 가능한가요?", a: "네. 디자인 토큰 시스템 기반이라 tokens.ts의 컬러/로고만 변경하면 전체 UI가 일괄 반영됩니다…" },
```

**수정 후:**

```ts
{ q: "도입 기업 브랜드로 커스터마이징이 가능한가", a: "네. 디자인 토큰 시스템 기반이라 tokens.ts의 컬러/로고만 변경하면 전체 UI가 일괄 반영된다…" },
```

**근거:**
1. §3.1 "우리" 제거 → `도입 기업`으로 객관화
2. §3.2.5 의문형 훅 `~인가요?` → 평서형 질문 `~인가` (판결문 어미, §4.1)
3. 답변 본문도 §3.2.1 `~입니다 → ~된다` 동일 평서형

**FAQ 일괄 전환 여부:** PR-03에서 전체 FAQ를 하나의 스타일로 통일할지 별도 논의.

---

## 체크리스트 (§8)

- [x] 감정 단어 없음
- [x] `~입니다` 없음 (본 PR에서 제거)
- [x] 감탄 부호 없음
- [x] 사주·한자·종교 없음
- [x] 숫자·직함·날짜 정확 (해당 없음)

**검증:** `grep -rn "우리 회사\|우리의\|우리는" src/` → 결과 0건이어야 승인.
